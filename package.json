{
  "name": "ignite-ui",
  "private": true,
  "packageManager": "npm@10.7.0",
  "workspaces": [
    "packages/*"
  ],

  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.1"
  }
}
